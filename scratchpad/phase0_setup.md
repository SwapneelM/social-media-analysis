# Phase 0: Project Setup

## Steps
1. [x] Create `.gitignore` — exclude `.env`, `__pycache__`, `node_modules`, `data/raw/`, etc.
2. [x] Create `requirements.txt` — `requests`, `python-dotenv`, `pytest`, `pytest-mock`
3. [x] Create `data/raw/{twitter,meta,tiktok}/` directories with `.gitkeep`
4. [x] Write `README.md` skeleton with project overview
5. [x] Log prompt to `swapneel_prompts.md`
6. [x] Git commit

## Notes
- `.env` already exists with API keys — must NEVER be committed
- BrightData dataset IDs still need to be added to `.env` by user
